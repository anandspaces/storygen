services:
  storygen-backend:
    container_name: storygen-backend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "6060:6060"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      # Optional: persist sqlite DB if you use one
      - ./data:/app/data
